# XXD pesi version
Currently only supports file formats ending in EOF. Will probably add some other formats I care about in the forseeable future. This is just a personal project. If you need a hexdump, just use actual xxd instead.
